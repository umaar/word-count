'use strict';

const wordCount = require('./word-count');
const sentence = 'Beware the Jabberwock, my son! The jaws that bite, the claws that catch!';
const result = wordCount(sentence);
console.log(result);
